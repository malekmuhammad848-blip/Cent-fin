name: Build Fully Functional Android APKs

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    env:
      NODE_VERSION: 20
      JAVA_VERSION: 21

    steps:
      # 1️⃣ Checkout code
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}

      # 3️⃣ Setup Java
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      # 4️⃣ Cache npm
      - name: Cache npm
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # 5️⃣ Cache Gradle
      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # 6️⃣ Install npm dependencies and build web assets
      - name: Build React/Vite project
        run: |
          cd Smartcent_Ready/www
          npm install
          npm run build

      # 7️⃣ Copy build output to Android assets/public
      - name: Copy build to Android assets
        run: |
          mkdir -p Smartcent_Ready/android/app/src/main/assets/public
          cp -r Smartcent_Ready/www/dist/* Smartcent_Ready/android/app/src/main/assets/public/

      # 8️⃣ Install Capacitor CLI
      - name: Install Capacitor CLI
        run: npm install -g @capacitor/cli

      # 9️⃣ Sync Capacitor Android
      - name: Sync Capacitor
        run: |
          cd Smartcent_Ready
          npx cap sync android

      # 🔟 Prepare keystore for signing
      - name: Prepare keystore
        run: |
          echo "$RELEASE_KEYSTORE" | base64 --decode > Smartcent_Ready/android/app/my-release-key.jks

      # 1️⃣1️⃣ Prepare gradlew
      - name: Prepare gradlew
        run: |
          cd Smartcent_Ready/android
          chmod +x gradlew
          sed -i 's/\r$//' gradlew

      # 1️⃣2️⃣ Build Debug APK
      - name: Build Debug APK
        run: |
          cd Smartcent_Ready/android
          ./gradlew assembleDebug --no-daemon --parallel --configure-on-demand

      # 1️⃣3️⃣ Build Signed Release APK
      - name: Build Signed Release APK
        run: |
          cd Smartcent_Ready/android
          ./gradlew assembleRelease \
            -PreleaseStoreFile=app/my-release-key.jks \
            -PreleaseStorePassword=$RELEASE_STORE_PASSWORD \
            -PreleaseKeyAlias=$RELEASE_KEY_ALIAS \
            -PreleaseKeyPassword=$RELEASE_KEY_PASSWORD \
            --no-daemon --parallel --configure-on-demand

      # 1️⃣4️⃣ Upload APKs as Artifacts
      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: android-apks
          path: |
            Smartcent_Ready/android/app/build/outputs/apk/debug/*.apk
            Smartcent_Ready/android/app/build/outputs/apk/release/*.apk
