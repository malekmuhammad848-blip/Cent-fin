name: Build Fully Functional Android APKs

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js 20
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Setup Java 21
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 21

      # Cache npm dependencies
      - name: Cache npm
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      # Cache Gradle
      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Extract ZIP
        run: |
          ZIP_FILE=$(find . -name "*.zip" -print -quit)
          if [ -z "$ZIP_FILE" ]; then
            echo "No ZIP file found!"
            exit 1
          fi
          unzip -o "$ZIP_FILE" -d extracted

      # Fix www/public folder if exists
      - name: Fix www/public folder
        run: |
          SRC="./extracted/Smartcent_Ready/www/public"
          DEST="./extracted/Smartcent_Ready/www"
          if [ -d "$SRC" ]; then
            mv $SRC/* $DEST/
            rm -rf $SRC
          fi

      # Go to www folder and build project (React/Vite)
      - name: Build React/Vite project
        run: |
          cd ./extracted/Smartcent_Ready/www
          npm install
          npm run build

      # Copy built www/dist to www root
      - name: Copy build to www root
        run: |
          cp -r ./extracted/Smartcent_Ready/www/dist/* ./extracted/Smartcent_Ready/www/

      # Install global Capacitor CLI
      - name: Install Capacitor CLI
        run: npm install -g @capacitor/cli

      # Sync Capacitor Android
      - name: Sync Capacitor
        run: |
          cd ./extracted/Smartcent_Ready
          npx cap sync android

      # Prepare keystore for signing
      - name: Prepare keystore
        run: |
          echo "$RELEASE_KEYSTORE" | base64 --decode > ./extracted/Smartcent_Ready/android/app/my-release-key.jks

      # Prepare gradlew
      - name: Prepare gradlew
        run: |
          ANDROID_DIR="./extracted/Smartcent_Ready/android"
          cd "$ANDROID_DIR"
          chmod +x ./gradlew
          sed -i 's/\r$//' ./gradlew

      # Build Debug APK
      - name: Build Debug APK
        run: |
          cd ./extracted/Smartcent_Ready/android
          ./gradlew assembleDebug --no-daemon --parallel --configure-on-demand

      # Build Signed Release APK
      - name: Build Signed Release APK
        run: |
          cd ./extracted/Smartcent_Ready/android
          ./gradlew assembleRelease \
            -PreleaseStoreFile=app/my-release-key.jks \
            -PreleaseStorePassword=$RELEASE_STORE_PASSWORD \
            -PreleaseKeyAlias=$RELEASE_KEY_ALIAS \
            -PreleaseKeyPassword=$RELEASE_KEY_PASSWORD \
            --no-daemon --parallel --configure-on-demand

      # Upload both APKs as artifacts
      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: android-apks
          path: |
            ./extracted/Smartcent_Ready/android/app/build/outputs/apk/debug/*.apk
            ./extracted/Smartcent_Ready/android/app/build/outputs/apk/release/*.apk
